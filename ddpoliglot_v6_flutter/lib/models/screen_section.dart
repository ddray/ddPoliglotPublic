import 'package:flutter/material.dart';

class ScreenSection {
  final int flex;
  final Alignment alignment;
  final double fontSize;
  final bool visible;
  final Color color;
  ScreenSection(
      this.flex, this.alignment, this.fontSize, this.color, this.visible);
}
