@page
@inject CommonLocalizationService localizer
@model ResetPasswordConfirmationModel
@{
    ViewData["Title"] = localizer.Get("xxReset password confirmation");
    ViewData["Noindex"] = true;
}

<!-- page title -->
<section class="page-title-section overlay" data-background="/img/backgrounds/page-title.jpg">
    <div class="container">
        <div class="row">
            <div class="col-md-8">
                <ul class="list-inline custom-breadcrumb">
                    <li class="list-inline-item"><a class="h2 text-primary font-secondary" href="@@page-link">@ViewData["Title"]</a></li>
                    <li class="list-inline-item text-white h3 font-secondary @@nasted"></li>
                </ul>
                <p class="text-lighten"></p>
            </div>
        </div>
    </div>
</section>
<!-- /page title -->
<div class="container mt-3 mb-3">
    <header>
        <h1>@ViewData["Title"]</h1>
        <p>
            @localizer.Get("xxYour password has been reset. Please ")<a asp-page="./Login">@localizer.Get("xxclick here to log in")</a>.
        </p>
    </header>
</div>

